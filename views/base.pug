doctype html
head
  meta(charset='utf-8')
  meta(content='width=device-width, initial-scale=1.0' name='viewport')
  title= title
  include _link

body
  include _header
  include _sidebar

  .row.d-flex.flex-column.align-items-center.justify-content-center
    #liveAlertPlaceholder.col-6.position-fixed.ms-5(style='z-index: 9999; margin-top:6rem;')

  main#main.main
    .pagetitle
      h1= title
      nav
        ol.breadcrumb
          li.breadcrumb-item
            a(href='/') Home
          each crumb, index in bread_crumbs
            li.breadcrumb-item(class= `${index === bread_crumbs.length - 1 ? 'active' : ''}`)= crumb

    // End Page Title
    
    block main

  // Scroll up
  a.back-to-top.d-flex.align-items-center.justify-content-center(href='#')
    i.bi.bi-arrow-up-short

  include _footer
